# Archive

This directory preserves older workspaces and transitional files that were used before the repository was reorganized around experiment-specific subdirectories.

## Contents

- `legacy_root_workspace/`
  - The original top-level disease embedding workspace before the project was split into:
    - `experiments/poincare_only/`
    - `experiments/poincare_hypstructure/`

## Intended Use

These files are kept for provenance and comparison.

They are not the recommended place to start new work. New development and reruns should use the maintained experiment directories under `../experiments/`.
