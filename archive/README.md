# Archive

This directory keeps slim historical provenance only.

## Contents

- `legacy_root_workspace/`
  - legacy script layout from before the repo was reorganized around dedicated experiment tracks

## Non-Goal

This directory is not a second working copy of the project.

Generated outputs, logs, metadata duplicates, and checkpoints were intentionally removed so the archive stays lightweight and readable.

## Recommended Use

Use this directory only when you need historical context for script evolution.

For actual work, start in:

- `../experiments/poincare_only/`
- `../experiments/poincare_hypstructure/`
